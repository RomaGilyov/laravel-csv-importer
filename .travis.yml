language: php

php:
    - 5.6
    - 7.0
    - 7.1
    - hhmv

matrix:
  include:
    - php: 5.6
      env: dependencies=lowest
    - php: 7
      env: dependencies=lowest
    - php: 7.1
      env: dependencies=lowest
    - php: 5.6
      env: dependencies=highest
    - php: 7
      env: dependencies=highest
    - php: 7.1
      env: dependencies=highest

services:
  - memcached
  - redis-server

before_script:
    - if [[ $TRAVIS_PHP_VERSION = hhvm ]]; then cat hhvm.php.ini >> /etc/hhvm/php.ini; fi;
    - echo "extension = memcached.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
    - echo "extension = redis.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
    - if [ -z "$dependencies" ]; then composer install; fi;
    - if [ "$dependencies" = "lowest" ]; then composer update --prefer-lowest -n; fi;
    - if [ "$dependencies" = "highest" ]; then composer update -n; fi;
    - php tests/Queue/run-queue-app-with-file-cache-driver.php &
    - php tests/Queue/run-queue-app-with-redis-cache-driver.php &
    - php tests/Queue/run-queue-app-with-memcached-cache-driver.php &

script:
  - vendor/bin/phpunit